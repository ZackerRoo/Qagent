# 两份文档对应数据服务接入（2026-09-14）

用户本轮明确要求接入基础服务与 ProMax 两个来源，并明确接受基础服务以 HTTP 明文传输密钥的风险。本轮在独立密钥、显式开关和原有运行预算下实施；不将目录项数量等同于全部业务适配或验证通过。本文记录实现与当前样本，最终全量测试、提交和部署证据由主任务续补。

## 两个来源及消费范围

| 来源 | 通用查询接入 | 实际业务消费 | 当前边界 |
| --- | --- | --- | --- |
| Datahubco 基础服务 | 文档目录 80 项，其中 79 项 HTTP 只读查询可通过客户端及单页研究 CLI 调用；`pro_bar` 为非 HTTP 项，显式拒绝 | 新增 `daily` 原始日线后备，位于既有来源和 ProMax 后备之后 | 79 项不是 79 项已验证，也不是已全部接入选股或模拟盘 |
| ProMax Relay | 复用既有目录查询、通用表格与研究适配 | 保留既有原始日线后备；当前基本面、分钟、显式锚定日线仍按既有研究边界使用 | 真实请求恢复尊重环境代理，不改变市场 5 秒预算，不宣称全接口稳定 |

基础客户端为 `backend/qagent/providers/datahubco.py`，固定文档目标、禁止重定向、错误脱敏，只有显式 `allow_insecure_http` 才发送请求。`scripts/collect_datahubco_research.py` 通过环境密钥及 `--allow-insecure-http` 获取一个显式请求的原始表格页，记录来源、取得时间、字段、行数及摘要；不连接数据库或账户，不自动翻遍接口。原始表格的业务语义、覆盖与历史 PIT 均需逐消费者核验。

原文此前只有 HTTP 且未授权发送密钥的记录继续保留为历史。本轮新增授权更新了允许调用的边界；没有把 HTTP 描述成加密或将该授权扩展到其他服务和接收方。两服务使用独立凭据，本文不含凭据。

## 真实调用证据

主任务按基础服务文档在 Mac 与云端分别取得同一条 `20260825` 日线，开盘 `11.57`、收盘 `11.59`；Mac 约 `0.40` 秒、云端约 `0.23` 秒，均成功返回 1 行。这是文档调用方式的单样本证据，不等于完整历史、全股票覆盖、归一化运行路径或部署后的自然任务验收。

主任务随后用新增 `DatahubcoMarketDataProvider` 在本地按运行 5 秒参数请求 `CN:000001`、起止同为 `2026-08-25`，成功得到 raw 日线，收盘 `11.59`、归一化成交量 `99,488,115` 股、来源 `datahubco_daily_raw`、`errors=[]`。这补齐了新归一化适配器的真实单样本，不代表云端生产自然触发或长期稳定性。

ProMax 先前本轮证据详见[文档调用方式核验](relay-document-method-20260914.md)：云端文档 cashflow 请求成功 HTTP 200、2 行，直连为 ConnectTimeout；Mac/云端的 daily、cashflow、rt_min 成败仍有差异。主任务云端隔离客户端在内存改用环境代理、30 秒超时、零重试，经正常目录及表格解析取得 `600519.SH`、`20241231` 现金流 2 行，总耗时 `7.03` 秒。此证据不能替代市场 5 秒可用性、同报告期现金流/利润配对或长期稳定性。

ProMax 返回的最近分钟样本仍是 `09-11 15:00`，不能当作 09-14 实时价格用于交易；新基础服务日线后备没有接入分钟或快照。原分钟链路保留，实时性与完整覆盖仍待验。

## 原始日线后备与发布

基础服务运行配置为 `QAGENT_DATAHUBCO_KEY`、`QAGENT_DATAHUBCO_ENABLED=true`、`QAGENT_DATAHUBCO_ALLOW_INSECURE_HTTP=true`，两个开关默认关闭；三条件齐备才构造后备。只补既有来源全部尝试后仍缺失的整只股票，不补已有股票内部日期；每次最多两只、单批、每请求 5 秒、零重试、30 秒启动预算，首次上游或结构失败停止本批并熔断 300 秒。

日线复用股票与日期身份、OHLC、重复行和有限数值校验，上海时间 15:00 前排除当日日线。成交量按手换股、成交额按千元换元，来源标识为 `datahubco_daily_raw`，调整价留空、类型 raw；不能填 G2 复权价缺口，也不提供分钟、快照或新的交易执行权限。它可影响未来日线消费者及既有日线降级估值/模拟执行，冻结模型、选股规则、账本历史和唯一账户均不改动。

ProMax 客户端的代理修正尊重 `HTTPS_PROXY`/`NO_PROXY` 等环境设置，保留固定 HTTPS、证书校验、请求头传密钥及禁止重定向；显式测试 transport 仍隔离环境。研究的文档 30 秒参数不提高运行后备的 5 秒上限。

主任务确认云端服务环境已有 `http_proxy`、`https_proxy`、`HTTP_PROXY`、`HTTPS_PROXY`、`NO_PROXY` 配置名称，本轮无需追加网络配置。代理值及凭据不在本文输出；存在配置仍不替代修订客户端部署后的实际选路验收。

`scripts/deploy_relay_runtime.py` 新增 `--enable-datahubco`，此模式从私密 stdin 接受两服务密钥 JSON，按明确授权配置基础服务 HTTP；不带此参数保留原 Relay 单密钥输入方式。沿用空闲检查、调度停止、writer 排空、环境原子替换、私密备份、健康检查、八表对账与失败回滚。实现和本地测试后仍须暂存 release 构建、受控切换及运行证据，不能从代码接线宣称线上已经启用。

## 验证与当前状态

运行后备及发布 helper 子任务相关回归 **71 passed、1 项既有 warning（2.92 秒）**，覆盖基础服务显式配置、来源、单位、预算、熔断、既有行情保留、双密钥输入与回滚；Ruff/diff 通过。ProMax 代理修正先前 **125 passed（1.62 秒）** 保留为其阶段证据，不能将两组重叠测试相加。基础通用客户端/研究 CLI 的最终测试和主任务后端全量正在由主任务汇总，未提前记录通过。

状态：两个来源的上述接入已实现，已完成上述本地相关验证；后端全量进行中，暂存及受控部署待验，尚未 commit、未 push、未部署本轮改动。实现发布后继续验自然任务覆盖、市场 5 秒可用性和财务配对，不宣称选股已改善。G3、G7 与 G2-FQ1 保持未完成，G1/G2 原门槛和历史快照保留。
