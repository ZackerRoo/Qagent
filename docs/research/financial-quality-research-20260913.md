# G2-FQ1 财务缺失诊断与现金流质量研究

日期：2026-09-13。属于 G2 的有界研究子项，不改变 G1–G7 的历史、冻结资格或验收门槛。先诊断现有财务输入，再隔离观察同报告期现金流与利润关系；是否改善选股仍待对照证实。

## 真实 09-11 信号缺失诊断

复用 `scripts/audit_g2_complete_cohort.py` 的来源摘要、股票身份、原始有限值及覆盖一致性校验，新增 `raw_feature_missingness`，保留既有输出字段和原始三路排序。财务组为 earnings_yield、return_on_equity、gross_margin、revenue_growth、earnings_growth，其余 12 项归市场组；此分组是指标语义分类，不是数据供应商分类，earnings_yield 同时包含财务和市场估值含义。

真实输入 `/tmp/qagent-selection-JBimpT/signal.json`，文件 SHA256 `d538386a89183f42b6a2b900be3405c338c034c014bd1fa4cd6e00d83ee6eef4`。本轮 CLI 退出 0，产物 `/tmp/qagent-selection-JBimpT/financial-missingness.json`，result_digest `728a6c2a4bff41c31bfd0beec16a3b4e8c89e8c89ba060a2b5160f61176f32ab`。逐特征、逐组及全部缺失组合均带完整股票 ID，方便复核。

| 范围 | 完整 / 5,541 | 完整率 | 至少一项缺失 |
| --- | ---: | ---: | ---: |
| 全部 17 项 | 3,518 | 63.4903% | 2,023 |
| 财务 5 项 | 3,522 | 63.5625% | 2,019 |
| 市场 12 项 | 5,454 | 98.4299% | 87 |

两组同时缺失 83 只；仅市场缺失 4 只，仅财务缺失 1,936 只。财务单项缺失：earnings_yield 1,883、return_on_equity 435、gross_margin 494、revenue_growth 446、earnings_growth 393。最大的非空完整缺失组合是仅 earnings_yield 缺失（1,433 只），其次仅全部五项财务缺失（311 只），仅 gross_margin 缺失（96 只）；另有市场和财务混合缺失组合，完整清单见 JSON，不能把这些组合数当作单项总数。

原始完整配对集合 Top10 中，三路分别有一只财务缺失：共识第 3 名 `CN:603208`、全特征第 10 名 `CN:688184`、去风险第 8 名 `CN:301100`，均仅缺 earnings_yield。本项报告原顺序上的身份，不重算子集 minimax，也不说明缺失造成排名差异。

**earnings_yield 缺失不能直接归为数据源缺数。** 主任务核对现有 engine：仅 `pe > 0` 时取 `1 / pe`，否则为 None；负 PE、零 PE 或真实缺数都可能落入缺失。当前归档 source 未包含原始 PE，不能从 1,883 个缺失判断具体原因。下一步优先区分非正 PE、亏损及真实缺数；不通过盲目补值提高覆盖，不修改冻结特征规则。

## 现金流研究假设与验收

候选假设：同报告期经营现金流与利润、收入的关系可能提供利润质量信息；这是待证假设，不是已证实的选股增强。独立 `scripts/research_cashflow_quality.py` 研究入口使用 cashflow 的 `n_cashflow_act` 与 income 的总净利润 `n_income`、`total_revenue`，限定同股票、同报告期、一般工商业 `comp_type=1`、合并累计报表 `report_type=1` 严格匹配，研究经营现金流/总净利润、经营现金流/收入。分母非正或缺失时保留原因，不以无穷值或零填充替代；不跨期相除，不将累计报表冒充单季或 TTM。保留来源、取得时间和摘要，不接 Challenger 正式权重，不生成排名。

上游字段语义参考 Tushare 官方[现金流量表](https://tushare.pro/document/2?doc_id=44)及[利润表](https://tushare.pro/document/2?doc_id=33)。官方文档不证明 Relay 返回完整或可用；研究脚本仍需校验股票身份、公告日期、报告期及重复修订。当前取得的公告日期与财报不能倒推历史可得性，2026-09-13 的数据不得回填到 09-11 冻结信号冒充 PIT。

完成条件：

1. 保留真实原始特征缺失审计及 Top10 身份，区分尚未确证的缺失原因；不修改冻结输入以追求更高覆盖。
2. 在最多两只股票的预算内完成现金流/利润当前观察，保留来源、取得时间、相同报告期、公告及修订冲突、无法计算原因；空表和错误也留证，不宣称覆盖完成。
3. 当前样本取得后先核验语义和覆盖，再设计同集合、同成本、同窗口的隔离对照；事前固定评价口径和未来样本，观察成熟收益前不改模型或排名，不据单日比率宣称增益。

本轮主任务通过云端执行只读请求：`600519.SH`、`603259.SH` 各查询 cashflow 和 income，`limit=10`、`report_type=1`、单次超时 5 秒、零重试。四次均为 `transport_error`、无行；这是本轮可用性未通过，不代表财报不存在，也未补齐任何现金流样本。脚本的 bounded live 入口与该次直接请求分别记录，不把直接请求称为完整脚本验收。

## 验证与交付状态

审计子任务运行现有审计、行为比较、共识排序相关测试 **42 passed（0.37 秒）**，Ruff 与 diff 检查通过；主任务独立复跑 **42 passed（0.30 秒）**、diff 检查通过。真实审计 CLI 退出 0，覆盖诊断已完成。新增测试覆盖财务/市场交叠、缺失模式计数及身份、原 Top10 顺序、输入不变、无缺失与非有限字符串、顺序不变性。现金流脚本最终测试另行补录，真实有效样本待上游恢复，不承诺本轮完成研究收益验证。

最终主任务验收：审计和现金流工具均已实现，组合相关测试 **57 passed（0.58 秒）**，包含现金流 **15 项专项测试**；Ruff 与 diff 检查通过。前段待补录保留为阶段快照，本段补齐最终本地验证状态；四次真实请求失败不变，尚无真实有效现金流样本。本轮未重跑全量、未 commit、未 push、未部署；没有改动冻结模型、原 signal、配对股票集合、生产排名或唯一模拟账户。G2-FQ1、G2 均未完成。
