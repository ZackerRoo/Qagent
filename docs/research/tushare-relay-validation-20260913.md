# ProMax / Tushare Relay 有界验证（2026-09-13）

结论：`000001.SZ` 在 `20260911` 的日线单行与现有云端缓存 OHLC 一致；复权因子、历史分钟及实时分钟本次均未验收通过。仅此样本不能证明来源独立、持续可用或适合替换生产行情。

## 范围与结果

依据用户提供的 Relay 手册第 1–9 节，以固定 HTTPS 地址发送单股票、有筛选条件的 GET，`__probe=0`、`limit=5`、单次请求 timeout 10 秒、不重试、不跟随跳转、保持 TLS 校验。历史日期固定为 `20260911`，分钟区间 09:30–15:00；实时接口查询当日，不套历史日期。Key 仅从环境变量读取，未写入文件或报告。此处为主任务实际调用的结果，不能用 mocked 测试代替。

| API | 本次证据 | 结论 |
| --- | --- | --- |
| daily | HTTP 200，code 0，1 行，日期 20260911，277 ms | 单样本日期有效；主任务追加同范围请求，OHLC 为 11.82 / 11.86 / 11.71 / 11.74，与云端 `CN:000001` 同日缓存精确相同 |
| adj_factor | transport_error，10,171 ms | 未取得有效结果；原分类未保留异常种类，不能确定为超时 |
| stk_mins | HTTP 202，原输出 upstream_error，5,794 ms | 未取得通过验收的分钟样本；未保留原始响应体，不能确证后台补数原因 |
| rt_min_daily | HTTP 503，8,270 ms | 本次不可用；当天为周日，不能验证交易时段实时性 |

主任务另查到已认证能力接口 HTTP 200、`enabled=true`、`provider=external`、`cache_ttl=300` 秒。这说明能力声明存在且配置有缓存，不能替代成功行情请求或延迟验收；300 秒缓存需纳入后续实时性评估。日线相等不证明两者使用独立上游；云端缓存来源为 `fuyao_realtime`，paired qfq。

## 实现与验证边界

新增隔离脚本 `scripts/probe_tushare_relay.py`，只输出安全元数据：API、HTTP、数值 code、行数、白名单字段名、数据日期范围、耗时和分类。校验代码、日期、重复键、日线/分钟 OHLC 的有限正数与高低边界、复权因子正数；分钟必须有完整时间。`sample_valid` 仅表示这些单样本检查通过，不是全量价格质量验收或实时性证明。HTTP 202 已在后续代码修订为 `pending_unverified`，上述首次实测保留原输出，不追改历史事实。

专项 mocked 测试 26 项通过，覆盖缺 Key、四次有界顺序调用、鉴权/限流/跳转/202、非 JSON、传输异常、代码与日期不符、重复键、异常 OHLC、复权因子及周末限制；Ruff 与 diff 检查通过。测试无联网、无真实 Key。首次实测使用脚本早期版本，之后增强的校验只有 mocked 验证，不声称全部已实测。

状态：已实现、已测试；未 push、未部署。未接入生产 provider、未修改价格信任规则、未修改数据库、冻结模型或单一模拟账户。G3 不提升为完成。下一步须在单股票预算内取得复权因子和分钟有效样本，再于交易时段核验有效行情时间及缓存延迟；不因本轮部分通过切换生产。
