# G2 完整特征子集诊断（2026-09-13）

现有两模型与共识 TopN 对照已完成，本轮补充对原始特征完整性的交叉审计。新增独立 `scripts/audit_g2_complete_cohort.py`，保留冻结实验的 5,541 个配对股票和既有排名，完整特征子集只用于附加诊断，不新增资格门禁、不删除冻结实验样本、不重算模型或子集 minimax。

入口校验 ready signal、嵌入 source 的双层摘要、冻结配置摘要、日期/扫描身份、股票集合与原始逐股特征。按既有有限数值转换语义重建陈旧日期、全缺失排除和逐特征覆盖，与 signal 的身份集合、逐行 `feature_coverage`、汇总覆盖交叉核对；行业须与嵌入 source 一致，行业非空计数也须吻合。缺少或矛盾的数据明确报 blocked，不能默认完整。此工具不重新验证完整 source 的 PIT、模型来源或独立前向资格，摘要仅证明内部一致。

共识复用 `rank_consensus` 先在完整配对集合生成原排序，再与两条基线一起过滤到同一完整特征集合。保留原始名次，复用行业统计和独占原子发布；输出完整集合/排除身份及缺失字段、集合摘要、三路 Top5/10/前10% 和原名单被过滤项。前10%的分母为完整特征子集，原名单过滤项的前10%分母仍为原配对集合，字段分开记录。完整数为零时明确 `no_complete_rows`，各名单为空，行业分布为 null。

```sh
python -S scripts/audit_g2_complete_cohort.py --signal /path/to/ready-signal.json --output /path/to/new-audit.json
```

## 真实信号结果

本地保留的 09-11 signal 实测退出 0：5,541 个配对股票中 3,518 个两变体原始特征共同完整，占 63.4903%；2,023 个仅从本附加诊断排除，完整 JSON 保留其身份及缺失字段。原 source 的 12 个陈旧日期排除独立记录，不与这 2,023 个混同。

| 完整特征子集 Top10 | 共识 | 全特征基线 | 去风险输入基线 |
| --- | --- | --- | --- |
| 名单数 | 10 | 10 | 10 |
| 最大行业占比 | 50% | 80% | 20% |
| 未知行业占比 | 0% | 0% | 0% |
| 原 Top10 因特征不完整移出诊断 | CN:603208 | CN:688184 | CN:301100 |

共识子集 Top10 为 `CN:603109, CN:301170, CN:301157, CN:001218, CN:688533, CN:603215, CN:603788, CN:300127, CN:301608, CN:688288`，原共识名次依次为 `1,2,4,5,6,7,8,9,10,11`。这些行业占比和名单差异不证明收益改善或风险下降。完整案例筛选会引入选择偏差，不能代表原完整配对集合，也不能据本对照推断缺失值导致了排名差异。

实测文件 `/tmp/qagent-selection-JBimpT/complete-cohort-final.json` 为临时可复现产物，原云端信号仍位于 `/var/lib/qagent-research/g2-forward-results/signals/2026-09-11.json`，本工具不修改输入。

主任务随后将完整产物以独占、不覆盖方式归档至云端 `/var/lib/qagent-research/g2-consensus-candidates/2026-09-11-complete-cohort-75d7b0bb18c9.json`，文件权限为 `0400`；本地与云端文件 SHA256 一致，为 `3e8db09bd0c8e70c5e6884908970865407462a18d069d157cc6c647bed56ba8a`。这是研究结果归档，未变更运行服务，也不表示脚本已部署到生产。

- 输入文件 SHA256：`d538386a89183f42b6a2b900be3405c338c034c014bd1fa4cd6e00d83ee6eef4`
- 完整集合 digest：`aac4e9c374b1e79b4ee8d0f739d473f7f0faadc869750dd0ddf9ed2da976f2ed`
- 本次产物 digest：`75d7b0bb18c90c73ee8a6666cdca51264125fa98925c4b88a3017f9816c0ed6b`

## 验证和交付状态

实施子任务运行本脚本与两份既有排序脚本的相关测试 **40 passed（0.40 秒）**，Ruff 通过，修订版真实 signal CLI 退出 0。用例覆盖 source/coverage/行业矛盾、缺失元数据、零完整样本、三路同集合、原始名次保留、输入不变、无推理依赖、摘要和不覆盖输出。主任务随后独立复跑 **40 passed（1.33 秒）**、Ruff 通过，以 `-S` 重新执行真实 CLI，并独立按原始特征复核完整 ID 集合和三路完整排序一致，最终产物 digest 与上述相同。本轮未跑全量、未 commit、未 push；研究产物已归档云端，脚本未部署到生产，独立离线工具无需生产部署。

`activation_allowed=false`、`decision_weight=false`、`forward_metrics=null`。尚无成熟收益、可执行性或效果结论；冻结实验、生产排序、唯一模拟账户、规则、历史和 G2 门槛保持原有定义。
